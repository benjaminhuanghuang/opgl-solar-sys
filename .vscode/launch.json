{
    "version": "0.2.0",
    "configurations": [
        {
            // for MacOS
            "name": "(lldb) Launch exe",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/cpp-version/solar.exe",
            "args": [],
            "cwd": "${workspaceRoot}/cpp-version",
            //"preLaunchTask": "cmake",
        }
    ]
}